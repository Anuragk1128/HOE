# Force npm usage and prevent pnpm
package-manager=npm
engine-strict=true
prefer-npm=true

# Disable pnpm and other package managers
auto-install-peers=false
legacy-peer-deps=true

# Ensure npm is used
registry=https://registry.npmjs.org/ 